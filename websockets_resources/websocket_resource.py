class WebSocketResource:
    """Class to implement the resources for web socket connections."""

    # property to get the namespace for the websocket
    signalling_server_namespace = "/signalling-server"
